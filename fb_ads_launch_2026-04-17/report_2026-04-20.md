```
📊 FB/IG Ads — Day 3 — 2026-04-20

AUDITORIA  (⏸ PAUSED)
  ayer:  $0.00 | 0 imp | 0 DMs | —/DM  (campaign paused, no delivery)
  total: $3.48 | 2,964 imp | 3 DMs | $1.16/DM

SITIO     (⏸ PAUSED)
  ayer:  $0.00 | 0 imp | 0 DMs | —/DM  (campaign paused, no delivery)
  total: $3.50 | 3,025 imp | 3 DMs | $1.17/DM

🏆 Winner so far: TIE (cost-per-DM within $0.01; neither beats the other by ≥30%)
🎯 Action: FIX — both campaigns are paused at the campaign level

REASON:
 · configured_status = PAUSED on both CAMPAIGN IDs. Ad sets + ads read ACTIVE
   but effective_status rolls down to CAMPAIGN_PAUSED → zero delivery yesterday.
 · Cumulative window (Apr 17–18 before pause): both camps crushed the $3/DM
   Day-3 scale-up threshold at ~$1.16/DM. If you want to follow the scale rule,
   un-pause first, then bump daily_budget from 500 (¢) → 1000 (¢).

EXACT API CALLS (run ONLY after Alex approves):

  # Un-pause AUDITORIA campaign
  curl -X POST "https://graph.facebook.com/v22.0/120240641300580662" \
    --data-urlencode "status=ACTIVE" \
    --data-urlencode "access_token=$TOKEN"

  # Un-pause SITIO campaign
  curl -X POST "https://graph.facebook.com/v22.0/120240641301630662" \
    --data-urlencode "status=ACTIVE" \
    --data-urlencode "access_token=$TOKEN"

  # (Optional, if scaling to $10/day after un-pause)
  curl -X POST "https://graph.facebook.com/v22.0/120240641661120662" \
    --data-urlencode "daily_budget=1000" \
    --data-urlencode "access_token=$TOKEN"
  curl -X POST "https://graph.facebook.com/v22.0/120240641706140662" \
    --data-urlencode "daily_budget=1000" \
    --data-urlencode "access_token=$TOKEN"
```

---

## Detail — what happened

**Launch window:** 2026-04-17 → 2026-04-18 (delivered ~1.5 days before the pause).
**Pause:** campaign-level pause is in place on both IDs (`120240641300580662` AUDITORIA, `120240641301630662` SITIO). No `issues_info` / no disapprovals — the pause is manual, not policy-driven.
**Yesterday's numbers (2026-04-19):** zero across the board because the pause was already in effect.

### Cumulative (Apr 17 → Apr 20 snapshot window)

| metric | AUDITORIA | SITIO |
|---|---|---|
| impressions | 2,964 | 3,025 |
| reach | 2,594 | 2,455 |
| spend | $3.48 | $3.50 |
| CPM | $1.17 | $1.16 |
| CPC | $0.14 | $0.15 |
| CTR | 0.84% | 0.76% |
| link clicks | 19 | 20 |
| DMs started (7d) | 3 | 3 |
| DM replies (7d) | 1 | 2 |
| DM blocks | 1 | 0 |
| cost / DM | $1.16 | $1.17 |

Both cost-per-DM values sit comfortably under the $3 Day-3 scale-up rule and far under the $8 kill threshold. SITIO has a slight quality edge (2 DM replies + 0 blocks vs AUDITORIA 1 reply + 1 block) but the sample is tiny (3 DMs each) — not enough to pick a winner yet.

### ManyChat reconciliation
Skipped this run — ManyChat contact-tag counts require a separate pull (manual or via ManyChat API). Recommend adding to tomorrow's run if the campaigns are un-paused. Expected tag counts: `auditoria_ad` and `sitio_ad` = 3 each over the full run.

### Scheduling window
Campaigns have `stop_time = 2026-06-16` (≈57 days away). No end-of-window action needed.

---

## Files saved this run

- `/sessions/bold-tender-clarke/mnt/jegodigital/fb_ads_launch_2026-04-17/insights_2026-04-20.json` — consolidated snapshot
- `/sessions/bold-tender-clarke/mnt/jegodigital/fb_ads_launch_2026-04-17/raw_2026-04-20/` — raw API responses (6 files)
