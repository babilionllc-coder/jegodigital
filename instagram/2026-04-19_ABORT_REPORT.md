# Daily IG Carousel — Sunday 2026-04-19 — ABORTED

**Topic (per schedule):** AI Lead Capture — Service 1 "Captura de Leads 24/7 con IA"
**Intended hook stats:** Flamingo 88% automated / Solik 95% qualify rate
**Intended proof screenshots:** Flamingo WhatsApp automation + GoodLife AI chatbot

**Status: NOT PUBLISHED. No slides rendered.**

---

## Why I stopped

Two independent blockers — either one alone is enough to abort per the skill's hard rules and the scheduled-task rule "if ANY step fails, report the error clearly — do not silently skip."

### Blocker 1 — All client screenshot folders are empty in this session

Every screenshot path referenced by the task and the `jegodigital-carousels` skill is missing:

| Folder | Image count |
|---|---|
| `flamingoscreenshots/` | 0 |
| `showcase/goodlife/` | 0 |
| `showcase/goza/` | 0 |
| `solik/` | 0 |
| `rsviajesscreenshots/` | 0 |

Folders exist, but they contain only `.DS_Store` files. The named files
(`flamingowhatsappautomationforleads.png`, `aichatbotthatclosesales24:7.png`,
`commandcenter.png`, etc.) do NOT exist anywhere in `/mnt/jegodigital`.

The skill is explicit:
> **NEVER fake mockups.** No CSS gradients pretending to be CRMs, dashboards, Google Maps, inboxes, charts. Use REAL screenshots only.
> If no real screenshot fits, **ASK Alex — do NOT fabricate.**

Without real screenshots for slides 2 and 3, the 5-slide structure collapses.
Fabricating CSS mockups would directly violate the skill's critical rules, and
shipping 3 slides instead of 5 would produce an off-spec deliverable.

### Blocker 2 — Instagram Graph API token is rejected

Verification call from the Graph API using the token embedded in `CLAUDE.md`:

```
GET https://graph.facebook.com/v22.0/17841424426942739?fields=username
→ error.code = 100, error_subcode = 33
→ "Object ... does not exist, cannot be loaded due to missing permissions,
   or does not support this operation"
```

Expected response was `{"username":"jegodigital",...}`. The task's Step 5
explicitly says: *"If token expired, STOP and report to Alex. Do NOT proceed
without valid token."*

---

## What Alex needs to do to unblock

1. **Sync the client screenshot library into this session's workspace.** The
   daily carousel job depends on `flamingoscreenshots/`, `showcase/goodlife/`,
   `showcase/goza/`, `solik/`, and `rsviajesscreenshots/` being populated.
   Either rsync them in from the main work machine or re-mount the folder
   that had them.

2. **Refresh the long-lived IG token.** The one currently embedded in
   `/mnt/jegodigital/CLAUDE.md` (Instagram Publishing section, starts with
   `EAAQdn3Rd3T0BRNX1ZBV...`) is no longer accepted for user `17841424426942739`.
   Mint a new long-lived user access token, update `CLAUDE.md`, and re-run.

3. **Re-run the scheduled task once above is fixed** — either manually trigger
   it or just wait for tomorrow's 00:00 run (topic shifts to Monday = SEO Local).

---

## What I did NOT do

- Did NOT fabricate any screenshots.
- Did NOT render any slides.
- Did NOT upload anything to litterbox/catbox.
- Did NOT call `media_publish`.
- Did NOT post anything to @jegodigital.

Nothing shipped. Nothing to roll back.

---

*Auto-generated by the daily-instagram-carousel scheduled task.
Session: sweet-epic-volta. Run date: 2026-04-19 (Sunday).*
