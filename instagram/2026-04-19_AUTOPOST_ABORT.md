# Daily Instagram Auto-Post — Sunday 2026-04-19 — ABORTED

**Scheduled task:** `instagram-ai-post` (one image from `instagram_queue`)
**Run time:** 2026-04-19 ~18:10 UTC
**Session:** magical-lucid-galileo

---

## Status: NOT PUBLISHED. Nothing posted. Firestore NOT mutated.

---

## What I picked up from the queue

| Field | Value |
|---|---|
| Doc ID | `dSrbI4kMCIllYCkfEynD` |
| `queuePosition` | 18 |
| `posted` | false (still false — left untouched) |
| `imageUrl` | `https://files.catbox.moe/zy5fuj.png` |
| `caption` | Pre-saved (997 chars). Hook: *"¿Mi nueva oficina? ¡A 40,000 pies de altura!"* |

Image was reachable (HTTP 200, 759 KB JPEG, 1024×1024). No issue with the asset.

---

## Why I stopped — Instagram Graph API token rejected

Verification call against the Graph API:
```
GET https://graph.facebook.com/v22.0/17841424426942739?fields=username&access_token=$TOKEN
```

Tried **both** tokens available in this session:

1. The one in `/mnt/OpenClaw/.env` `INSTAGRAM_ACCESS_TOKEN` (starts `EAAQdn3Rd3T0BQ6aFQ...`)
2. The one in `/mnt/jegodigital/CLAUDE.md` Instagram Publishing section (starts `EAAQdn3Rd3T0BRNX1ZBV...`)

Both return:
```
error.code = 100, subcode = 33
"Object with ID '17841424426942739' does not exist, cannot be loaded
 due to missing permissions, or does not support this operation"
```

This is the **same blocker** that aborted the morning carousel run — see
`2026-04-19_ABORT_REPORT.md`. The token has not been refreshed yet.

I also checked Google Secret Manager for a fresher token — the service account
in this session does not have `secretmanager.versions.access`, so no rotation
path from inside the sandbox.

---

## Why Firestore was not updated

Step 5 of the scheduled task says "Always complete Step 5. Never skip it." —
but the docstring for Step 5 lists fields that mark the post as **shipped**
(`posted: true`, `instagramPostId`, `postedAt`). Marking the doc shipped
when nothing was published would corrupt the queue: the next run would skip
position 18 forever and the post would never go out.

The doc is left as `posted: false` so the next successful run picks it up.

---

## What Alex needs to do to unblock

1. **Mint a new long-lived Instagram Graph API user token** for `@jegodigital`
   (IG user `17841424426942739`).
2. Update **both**:
   - `/mnt/jegodigital/CLAUDE.md` — Instagram Publishing section
   - `/mnt/OpenClaw/.env` — `INSTAGRAM_ACCESS_TOKEN`
   (and the `meta_tokens.md` auto-memory file).
3. Re-run this scheduled task. Doc `dSrbI4kMCIllYCkfEynD` (queue pos 18,
   "40,000 pies de altura" caption) is still next in line.

## Queue health

- 9 unposted docs remaining (positions 18, 37, 38, 39, 40, ...) — **WARN: ≤5
  soon if the token blocker isn't resolved.** Positions 19–36 are already
  posted, so the next batch waiting is the Fort Lauderdale series at 37–40.

## What I did NOT do

- Did NOT publish anything to @jegodigital.
- Did NOT update the Firestore doc.
- Did NOT call Gemini (caption was pre-saved — would have been skipped anyway).
- Did NOT fabricate a fake "success" log.

---

*Auto-generated by `instagram-ai-post` scheduled task.
Same root cause as `2026-04-19_ABORT_REPORT.md` (carousel job).
One fix — refreshing the IG token — unblocks both daily jobs.*
